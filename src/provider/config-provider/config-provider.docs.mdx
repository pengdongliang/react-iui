---
description: 全局化配置
labels: ['ConfigProvider']
---

### 功能

- 为组件提供统一的全局化配置

### Props

| 参数 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| namespace | 组件的样式类名的前缀 | string | 'ant' |

### 示例

import { ConfigProvider } from './config-provider'

```tsx live
<ConfigProvider />
```
